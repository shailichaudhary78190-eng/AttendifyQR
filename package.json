{
  "name": "qr-attendance",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:full": "concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "vercel-build": "npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
